<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Perfil - Actividad 4 - CRUD</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="fullscreen">
    <header class="top-header">
        <h1>Editar Mi Perfil</h1>
        <p>Actualiza tu información personal</p>
    </header>
    
    <%- include('partials/nav') %>
    
    <main class="main-content">
        <% if (typeof errors !== 'undefined' && errors && errors.length > 0) { %>
            <div class="error-message">
                <strong>Por favor, corrige los siguientes errores:</strong>
                <ul>
                    <% errors.forEach(function(error) { %>
                        <li><%= error %></li>
                    <% }); %>
                </ul>
            </div>
        <% } %>
        
        <% if (typeof success !== 'undefined' && success) { %>
            <div class="success-message">
                <%= success %>
            </div>
        <% } %>
        
        <div class="form-container">
            <form method="POST" action="/profile/edit" id="profileForm" novalidate>
                <div class="form-group">
                    <label for="tipoDocumento" class="form-label">Tipo de documento *</label>
                    <select 
                        name="tipoDocumento" 
                        id="tipoDocumento" 
                        class="form-select"
                        required
                    >
                        <option value="">Selecciona un tipo</option>
                        <option value="CC" <%= (meta && meta.tipoDocumento === 'CC') ? 'selected' : '' %>>Cédula de Ciudadanía (CC)</option>
                        <option value="TI" <%= (meta && meta.tipoDocumento === 'TI') ? 'selected' : '' %>>Tarjeta de Identidad (TI)</option>
                        <option value="CE" <%= (meta && meta.tipoDocumento === 'CE') ? 'selected' : '' %>>Cédula de Extranjería (CE)</option>
                        <option value="PAS" <%= (meta && meta.tipoDocumento === 'PAS') ? 'selected' : '' %>>Pasaporte (PAS)</option>
                        <option value="NIT" <%= (meta && meta.tipoDocumento === 'NIT') ? 'selected' : '' %>>NIT</option>
                    </select>
                    <span class="field-error" id="tipoDocumento-error"></span>
                </div>
                
                <div class="form-group">
                    <label for="numeroDocumento" class="form-label">Número de documento *</label>
                    <input 
                        type="text" 
                        name="numeroDocumento" 
                        id="numeroDocumento"
                        class="form-input"
                        value="<%= (meta && meta.numeroDocumento) || '' %>"
                        placeholder="Ingresa tu número de documento"
                        pattern="[0-9]{6,15}"
                        title="Debe contener entre 6 y 15 números"
                        required
                    >
                    <span class="field-error" id="numeroDocumento-error"></span>
                </div>
                
                <div class="form-group">
                    <label for="direccion" class="form-label">Dirección</label>
                    <input 
                        type="text" 
                        name="direccion" 
                        id="direccion"
                        class="form-input"
                        value="<%= (meta && meta.direccion) || '' %>"
                        placeholder="Calle 123 #45-67, Barrio, Ciudad"
                        minlength="5"
                        maxlength="100"
                    >
                    <span class="field-error" id="direccion-error"></span>
                </div>
                
                <div class="form-group">
                    <label for="telefono" class="form-label">Número de teléfono</label>
                    <input 
                        type="tel" 
                        name="telefono" 
                        id="telefono"
                        class="form-input"
                        value="<%= (meta && meta.telefono) || '' %>"
                        placeholder="+57 300 123 4567"
                        pattern="^[+]?[0-9\s\-()]+$"
                        title="Solo números, espacios, guiones, paréntesis y el símbolo +"
                    >
                    <span class="field-error" id="telefono-error"></span>
                </div>
                
                <div class="form-group">
                    <button type="submit" class="btn btn-success" id="submitBtn">
                        Guardar cambios
                    </button>
                    <a href="/profile" class="btn btn-secondary">
                        Cancelar
                    </a>
                </div>
            </form>
        </div>
    </main>
    
    <script src="/js/app.js"></script>
</body>
</html>
