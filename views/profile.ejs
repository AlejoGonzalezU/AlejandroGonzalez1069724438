<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Perfil - Actividad 4 - CRUD</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="fullscreen">
    <header class="top-header">
        <h1>Mi Perfil</h1>
        <p>Información de tu cuenta y datos personales</p>
    </header>
    
    <%- include('partials/nav') %>
    
    <main class="main-content">
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="error-message">
                <%= error %>
            </div>
        <% } %>
        
        <div class="profile-card">
            <h2>Información de la cuenta</h2>
            
            <div class="profile-section">
                <h3>Datos básicos de Auth0</h3>
                <p><strong>Email:</strong> <%= user.email || 'No disponible' %></p>
                <p><strong>Nombre:</strong> <%= user.name || 'No configurado' %></p>
                <p><strong>Usuario:</strong> <%= user.nickname || 'No disponible' %></p>
                <p><strong>Verificado:</strong> <%= user.email_verified ? 'Sí' : 'No' %></p>
                <p><strong>Última actualización:</strong> <%= user.updated_at ? new Date(user.updated_at).toLocaleString('es-ES') : 'No disponible' %></p>
            </div>
            
            <% if (user.user_metadata && Object.keys(user.user_metadata).length > 0) { %>
                <div class="profile-section">
                    <h3>Información personal</h3>
                    <% if (user.user_metadata.tipoDocumento) { %>
                        <p><strong>Tipo de documento:</strong> <%= user.user_metadata.tipoDocumento %></p>
                    <% } %>
                    <% if (user.user_metadata.numeroDocumento) { %>
                        <p><strong>Número de documento:</strong> <%= user.user_metadata.numeroDocumento %></p>
                    <% } %>
                    <% if (user.user_metadata.direccion) { %>
                        <p><strong>Dirección:</strong> <%= user.user_metadata.direccion %></p>
                    <% } %>
                    <% if (user.user_metadata.telefono) { %>
                        <p><strong>Teléfono:</strong> <%= user.user_metadata.telefono %></p>
                    <% } %>
                </div>
            <% } else { %>
                <div class="profile-section">
                    <h3>Información personal</h3>
                    <p>No has agregado información personal aún. <a href="/profile/edit">Haz clic aquí para agregar tus datos</a></p>
                </div>
            <% } %>
        </div>
    </main>
    
    <script src="/js/app.js"></script>
</body>
</html>
